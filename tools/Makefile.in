CC=@CC@
RM=@RM@
TCMLIBS=@LIBS@
INCLUDEDIR=-I../include

.c.o:
	${CC} ${CPPFLAGS} ${CFLAGS} -c $<

all: mkpasswd

mkpasswd: mkpasswd.o
	$(CC) $(LDFLAGS) -o mkpasswd mkpasswd.o ${TCMLIBS}

clean:
	${RM} -f mkpasswd *.o *~ *core

distclean: clean
	${RM} -f Makefile

